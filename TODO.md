# MC Web Panel 优化与演进路线图 (TODO Roadmap)

为了进一步提升面板的性能、安全性、开发者友好度以及小白用户的上手体验，特制定以下优化路线图：

---

## 🛠️ 1. 架构与性能优化 (Architecture & Performance)
- [ ] **`server.js` 单体路由模块化拆分**
  - [ ] 按照业务模块将 `server.js` 的接口拆分至 `routes/` 目录下（如 `auth.js`、`instances.js`、`files.js`、`plugins.js` 等）。
  - [ ] 建立 `controllers/` 层，使路由层与业务逻辑层分离，提高代码的可维护性与单体测试覆盖率。
- [ ] **高负载 CPU 密集型任务解耦**
  - [ ] 引入 Node.js 的 `Worker Threads`（工作线程）或子进程，将 ZIP 文件解压、备份压缩和多版本冲突深度扫描移出主事件循环，防止 Event Loop 阻塞造成面板界面瞬间卡顿。
  - [ ] 建立异步任务进度监视器，前端通过 WebSocket 实时拉取进度。
- [ ] **面板状态与临时缓存写盘持久化**
  - [ ] 引入轻量级无依赖的本地持久化缓存（如 JSON 读写组件或 `lowdb`），将 IP 爆破封禁到期时间、临时分片上传状态等存盘，保证面板重启后状态不丢失。

---

## 🛡️ 2. 系统可靠性与崩溃诊断 (Reliability & Diagnostics)
- [ ] **Java 服务进程崩溃特征诊断 (小白关怀)**
  - [ ] 在后端监控 `stdout` / `stderr` 输出流，检测 JVM 内存溢出 (`OutOfMemoryError`) 或 Mod 崩溃特征码。
  - [ ] 发生异常闪退时，面板提示框以通俗语言指导服主：“检测到由于分配内存不足导致闪退，建议在‘设置’中调大 Java 内存。”
- [ ] **物理内存智能探测分配推荐**
  - [ ] 结合 `systeminformation` 库，在“设置 Java 内存”时探测服务器总物理内存，提供“安全推荐分配值”，防止小白服主设置过大内存导致系统 OOM 崩溃。

---

## 🔒 3. 安全防护与审计加固 (Security Hardening)
- [ ] **Zip Slip 路径穿越漏洞防御**
  - [ ] 对模组上传、备份解压中的文件名 Entry 强制实施 `path.startsWith` 归属限制校验，防止恶意压缩包覆盖系统核心文件。
- [ ] **管理员操作审计日志 (Audit Log)**
  - [ ] 建立独立的 `logs/audit.log` 审计日志，记录所有核心操作（如启停服务、修改端口、安装/卸载插件、清除 2FA 等）对应的账号名、IP 与执行时刻，保证操作可追溯。

---

## 🔌 4. 插件沙盒与开发者生态 (Plugins & Developer Ecosystem)
- [ ] **第三方插件异常隔离与防崩沙箱**
  - [ ] 针对插件路由装载与挂载逻辑，加入防崩 `try-catch` 沙盒保护，使得任何报错或抛出的未捕获异常均不会导致面板主体进程闪退。
- [ ] **插件清单规格化 Schema 校验**
  - [ ] 在加载插件之前，通过 JSON Schema 严格校验其 `plugin.json` 元数据格式，防止格式混乱的插件导致面板初始化死锁。

---

## ✨ 5. 极致体验与极简交互 (UX & Easy-to-use)
- [ ] **前端原生 ESM 模块 HTTP 静态缓存**
  - [ ] 配置服务端对 `/js/components/` 下的 Vue 组件静态文件返回合理的 `Cache-Control`，减少页面切换时的多重网络请求，实现页面极速响应，消除闪烁感。
- [ ] **文件夹拖拽直传 (Directory Upload)**
  - [ ] 在文件管理中集成 `webkitdirectory`，支持拖拽一整个本地文件夹自动在服务器创建目录并层级上传，极大地方便小白用户部署整合包。
