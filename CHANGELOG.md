# MC Web Panel 更新日志

## [2.4.4] - 2026-09-13

### 🛠️ 优化与修复

- **单人快速跳过夜晚卷轴 (`fast-sleep` v1.3.0) 核心机制重构与全链路客户端静默**：
  - **调试日志管道接入与实时呈现**：修复 `scroll-engine.js`，将卷轴的 `logger.info/warn/error` 实时推送到面板控制台 `appendLog` 管道及日志文件中，彻底解决卷轴在面板控制台静默、无调试输出的问题；
  - **单次就绪生效机制（彻底消除无意义轮询与进服重复下发）**：
    - 因 Minecraft `/gamerule` 会直接写入世界持久化数据（`level.dat`）永久生效，彻底移除了原每 30 秒重复执行的 `schedule(30000)` 轮询定时器与玩家进服重设规则逻辑；
    - 新增 `ruleApplied` 状态守卫：仅在服务器已在运行中（`scroll.isServerRunning()`）或开服就绪完成（`scroll.onServerStart`）时执行一次；
  - **全链路自动静默客户端指令回显**：
    - 彻底根治 Minecraft 原版 `sendCommandFeedback true` 导致的客户端聊天栏频繁弹出 `[Server: 已将游戏规则playersSleepingPercentage设为1]`、`[Server: 已将xxx传送至...]` 等系统回显刷屏问题；
    - 服务端就绪进入 `running` 状态时，`server.js` 自动向进程写入 `gamerule sendCommandFeedback false\n`；
    - 卷轴引擎在 `handleServerStart` 中自动下发静默指令，并在上下文沙盒注入 `scroll.isServerRunning()` 与 `scroll.silenceCommandFeedback()` 辅助函数；
    - `fast-sleep` 与 `safe-world` 在执行原生规则或切换白昼天气（`time set day` / `weather clear`）前自动确保客户端回显关闭；
    - 玩家在游戏内的聊天栏恢复整洁清爽，面板网页控制台和日志文件不受任何影响，完整记录所有操作；
  - **全版本交互与成就监听**：健全玩家聊天快捷指令（`!sleep`、`!day`、`/sleep`、`睡觉`、`晚安`、`跳过夜晚` 等）与原版 `[Sweet Dreams]` / `[甜蜜的梦]` 进度及入睡日志捕获；卷轴卸载时自动恢复 `/gamerule playersSleepingPercentage 100`。

- **第三方皮肤账号（LittleSkin / Ely.by 等离线/外置登录）头像全链路兼容与智能优先级降级**：
  - **根因修复**：解决 `mc-heads.net` 接收到第三方账号时返回 HTTP 200 默认 Steve 头像导致后续 LittleSkin、Ely.by 等国内/国际主流第三方皮肤源被提前短路截断的问题；
  - **后端 `/api/avatar` 优先级链路重构**：
    - 重构最优请求顺序：`crafthead.net`（官方正版极速，不存在返回 404） → `littleskin.cn`（国内最大第三方皮肤站） → `skinsystem.ely.by`（国际主流第三方皮肤站） → `minotar.net`（正版备用源） → `mc-heads.net`（兜底源）；
    - 支持 `?refresh=1` / `?force=1` 强制绕过本地磁盘缓存重新拉取最新皮肤；
    - 自动清理 `data/avatar_cache/` 中被旧逻辑污染的默认 Steve 缓存图片；
  - **前端与地图联动降级**：前端 `Avatar.js` 及地图图钉 Marker 的 `onerror` 降级链路同步按该最优顺序排列，确保第三方账号玩家的个性化头像能 100% 正确呈现。

- **服务器地图插件 (mc-panel-plugin-map) 多维度支持、下界 Y 轴切片高度透视与跨维度传送修复 (v1.3.0)**：
  - **多维度地图获取与玩家维度显示**：
    - 修复后台玩家实体维度查询缺失问题，在定时器中补充 `data get entity <player> Dimension` 查询；
    - 修复维度 NBT 正则匹配：支持带双引号及命名空间（如 `"minecraft:the_nether"`、`"minecraft:the_end"`、模组自定义维度等）；
    - 增强 `getRegionPath`：兼容各类服务端核心（Vanilla/Fabric 的 `DIM-1`/`DIM1` 与 Paper/Spigot 的 `world_nether`/`world_the_end` 等）；
    - 彻底修复前端切换维度时 `resetView()` 误用 `panToPlayer` 将维度反向重置为主世界的恶性 Bug；
    - 新增当前维度暂无已生成区块的友好提示；
  - **下界地形透视：自定义 Y 轴切片高度选择器**：
    - 针对下界 Y=100~127 全为致密实心下界岩与基岩顶盖导致俯视图呈现一片红色岩壁的问题，在 `renderRegionToPNG` 中引入 `maxY` 切片高度；
    - 下界默认切片高度设定为 **`Y=76`**（直接穿透顶盖致密层，切入下界要塞、猪灵堡垒、主要活动层与岩浆海）；
    - `/render` 路由将瓦片缓存与并发锁按 `${instanceId}_${dim}_y${maxY}_${region}.png` 严格隔离，切换高度秒级换图且互不干扰；
    - `/safe-surface-y` 接口同步适配 `maxY`，在指定切片高度下方寻找可站立的实体表面；
    - 前端顶部维度工具栏新增 Y 轴切片高度下拉选择器，提供下界、主世界与末地的丰富预设（如岩浆海平面、深层板岩层等）；
  - **跨维度传送指令彻底修复**：
    - 修复因控制台默认上下文为主世界导致在下界/末地点击传送玩家误落回主世界的 Bug；
    - 传送执行函数全量改用标准跨维度语法 `/execute in <dimension> run tp <player> <X> <Y> <Z>`（随机传送同步改用 `/execute in <dimension> run spreadplayers ...`）；
    - 传送弹窗与确认框明确标识目标维度与安全坐标。

- **世界安全与防爆守护卷轴 (`safe-world` v1.1.0)**：
  - 增加 `ruleApplied` 守卫与单次就绪生效机制，避免重复执行；
  - 下发规则前执行 `gamerule sendCommandFeedback false`，保持客户端完全静默；
  - 重新打包发布至商店并更新索引。

- **官方插件与卷轴商店索引发布包同步构建**：
  - 重新打包发布 `docs/plugins_shop/mc-panel-plugin-map.zip` (v1.3.0) 并更新 `plugins.json`；
  - 重新打包发布 `docs/scrolls_shop/fast-sleep.zip` (v1.3.0)、`safe-world.zip` (v1.1.0) 并更新 `scrolls.json`。

## [2.4.3] - 2026-09-12

### 🛠️ 优化与修复

- **面板全局备份与系统还原全链路重构与加固 (彻底解决丢配置、丢文件、还原损坏)**：
  - **原生流式解压与权限软链无损还原**：重构 `extractBackupArchive`，优先调用系统原生 `unzip -qo`（自动重置 `UNZIP=''` 环境变量），原生保留所有 POSIX 0755/0644 权限与符号链接，避免将大文件读入 V8 堆内存造成 OOM；第二级智能降级至原生 `tar -xf`；第三级降级至 AdmZip 并从 entry.attr 恢复文件权限与符号链接，并在 Linux 环境下自动校验并修复 `data/java/**/bin/java` 的可执行权限；
  - **关键配置子目录全量打包**：彻底解决原备份仅打包 `data/*.json` 顶层文件导致 `appearance/`（外观主题）、`frp/`（内网穿透）、`scroll_storage/`（卷轴持久化存储）丢失的缺陷，全面打包所有子目录并排除临时缓存；
  - **世界安全刷盘与嵌套备份递归排除**：备份前向运行中的服务端发送 `save-off` 与 `save-all flush` 确保世界区块完整落盘；打包实例时严格过滤 `backups/` 目录与 `panel.log`，防止压缩包无限递归膨胀；
  - **Stream Pipeline 分片装配**：分片合并改写为基于 Node.js 流式管道 (`stream/promises pipeline`) 拼接，彻底解决大文件背压截断与末尾损坏；
  - **登录界面灾难恢复模式与多重别名路由**：
    - 修复登录界面因调用 `/api/backups/global/...` 导致 404 错误的问题，注册全套别名路由；
    - 改造 `requireBackupAccess` 鉴权中间件，支持携带 `x-admin-password` 在未登录状态下验证管理员密码执行灾难恢复；
    - 登录界面新增「灾难恢复 / 从备份还原」模式，支持查看待回档文件名与文件大小、动态上传进度展示与一键回档。

- **卷轴工坊：单人快速跳过夜晚 (`fast-sleep` v1.1.0) 与健康防沉迷守护 (`health-guardian` v1.1.0) 重构**：
  - **`fast-sleep` v1.1.0**：启动自动下发 Minecraft 1.17+ 原生规则 `/gamerule playersSleepingPercentage 1`（卸载自动恢复 100）；支持聊天输入 `!sleep`、`!day`、`睡觉`、`晚安` 快捷跳夜；支持原版 `[Sweet Dreams]` / `[甜蜜的梦]` 进度与中英文入睡日志；支持全屏大标题与破晓音效；
  - **`health-guardian` v1.1.0**：启动即刻通过 `scroll.getOnlinePlayers()` 同步当前所有在服玩家；使用 `scroll.storage` 持久化 `sessions`，离线重连连续累计时长；ActionBar + tellraw 私聊 + 拾取音效三重提醒；支持聊天栏输入 `!health`、`!防沉迷` 随时查询在线时长与下次提醒倒计时；
  - **卷轴引擎与生命周期增强**：`scroll-engine.js` 扩展 `sendMessage` 与 `playSound` 快捷接口；日志分发统一提供 `line` 与 `raw` 字段；在服务端状态切为 `running` 时触发 `onServerStart`，在 `stopping` 和进程退出时触发 `onServerStop`；
  - 重新打包发布 `docs/scrolls_shop/fast-sleep.zip` 与 `health-guardian.zip`，更新 `docs/scrolls_shop/scrolls.json`。

- **语音聊天插件 (Simple Voice Chat) 全方位配置适配与升级 (v1.1.0)**：
  - **状态判定与加载时序修复**：修复前端检查 `store.stats.hasVoicechat` 导致始终误判为“未安装模组”且拦截配置加载的严重问题，实现 `store.hasVoicechat` 与 `store.stats.hasVoicechat` 双向无缝同步；支持即使模组尚未启动也可一键强制打开配置；
  - **智能多路径嗅探与零 404 兜底**：
    - 后端增加多环境、多加载器配置文件自动探测（覆盖 `config/voicechat/voicechat-server.properties`、`config/voicechat.properties`、`plugins/voicechat/` 等路径）；
    - 若服务器尚未首次启动生成该文件，自动提供官方标准的默认配置模板（标明 `exists: false`），彻底消除 404 报错与 `# Error reading config` 页面覆盖异常；
    - 保存配置时自动创建对应父级目录，支持向后写入真实探测路径。
  - **全属性 GUI 配置模型重构**：
    - 废除原界面中与 Simple Voice Chat 不符的废弃字段；
    - 完整覆盖官方所有核心配置属性（网络连接、语音距离、耳语距离、广播范围、Opus 编码器、MTU 包大小、TCP 速率限制、群组开关、录音权限、旁观者通话、强制模组校验、原生动态库加速、多线程 Folia 支持等）；
    - 智能兼容 `max_voice_distance` 与旧版 `voice_distance` 别名映射；
  - **高保真双向转换引擎**：GUI 模式与文本模式切换或保存时，精确替换目标项并无损保留配置文件中的原版注释说明与结构；
  - **全量多语言国际化**：在宿主 `i18n.js` 中补齐所有语音配置项的中英文双语翻译。

- **认证管理插件 (EasyAuth) 数据库读取、Schema 适配与密码维护修复 (v1.2.0)**：
  - **解除前端挂载与刷新阻断**：修复前端因检查未初始化的 `store.stats.hasEasyAuth` 导致组件挂载时不调用 `loadUsers()`、界面锁定为“未安装模组”的缺陷；
  - **Linux 大小写与动态路径兼容查找**：
    - 优先解析 `config/EasyAuth/storage.conf`，读取自定义的 `sqlite-path` 与 `sqlite-table`；
    - 候选扫描全面兼顾 Linux 大小写敏感特性（同时支持 `EasyAuth/` 与 `easyauth/` 目录）；
    - 增加备用目录 `.db` 文件自动兜底探测。
  - **EasyAuth 真实表结构与 `data` JSON 列适配**：
    - 针对 EasyAuth 表中无独立 `password` 列、密码与鉴权数据存于 `data` JSON 列的真实结构进行重构；
    - 查询玩家列表时自动解析 `data` JSON，提取真实注册状态、最近登录 IP、最近活跃时间及注册时间；
    - 彻底修复修改密码时因执行 `UPDATE easyauth SET password = ?` 抛出 `SQLITE_ERROR: no such column: password` 导致的 500 异常，实现对 `data` JSON 列的反序列化、BCrypt 哈希注入与无损回写；
    - 注销/删除玩家增加 `username` 与 `username_lower` 大小写双重匹配。
  - **SQLite 执行高可用双引擎降级**：
    - 默认优先使用 Node 原生 `sqlite3` 模块；
    - 若遇环境或 ABI 限制，自动降级启用系统原生 `sqlite3` CLI，确保任何环境下均能百分之百稳定读取数据库。
  - **UI 呈现升级**：新增当前数据库路径状态徽章、玩家最后登录 IP / 注册时间展示，并在无数据时给出明确的注册指引提示。

- **服务器地图插件 (mc-panel-plugin-map) 玩家雷达头像加载与动态校准修复 (v1.1.1)**：
  - **地图雷达头像源对齐**：彻底修复地图内标记硬编码请求 `minotar.net` 导致的非正版/离线皮肤无法加载、退化为默认 Steve 面孔的问题；全面对齐宿主面板内置的 `/api/avatar` 接口（具备本地磁盘缓存与多源极速容灾机制）；
  - **多源优雅容灾回退链路**：地图标记增加 `onerror` 级联回退机制（依次尝试 `/api/avatar` → Crafthead → LittleSkin → MC-Heads → 首字母圆标），确保任何网络环境下均能平滑展现；
  - **玩家雷达标记动态同步增强**：在轮询更新玩家坐标时，实时同步更新高度 Y 坐标徽章、在线状态脉冲光圈、头像边框颜色与头像资源地址，并支持点击后定位最新动态位置。

- **官方插件商店索引更新与发布包构建**：
  - 同步更新 `mc-panel-plugin-voicechat` 至 `v1.1.0`，重新压缩打包 `mc-panel-plugin-voicechat.zip`；
  - 同步更新 `mc-panel-plugin-easyauth` 至 `v1.2.0`，重新压缩打包 `mc-panel-plugin-easyauth.zip`；
  - 同步更新 `mc-panel-plugin-map` 至 `v1.1.1`，重新压缩打包 `mc-panel-plugin-map.zip`；
  - 同步更新 `docs/plugins_shop/plugins.json` 索引信息并同步至 `runner/` 与 `dist/temp/` 目录。

---
## [2.4.2] - 2026-09-11

### ✨ 新功能

- **实例全生命周期「四状态」精细化管理体系 (Four-State Lifecycle Management)**：
  - **由「两态」升级为「四态」过渡管理**：彻底告别以往单纯的「运行中 / 已停止」粗粒度状态，细分为 `启动中 (starting)`、`运行中 (running)`、`停止中 (stopping)` 与 `已停止 (stopped)` 四个全生命周期状态，直观反映服务端底层进程与游戏循环的真实推进阶段。
  - **深度兼容各大主流核心与模组加载器**：
    - **纯净版 (Vanilla) & 插件服 (Paper / Purpur / Spigot)**：精准捕获 `Done (XXs)! For help, type "help"` 及 Timings 等就绪特征信号；
    - **Fabric 模组服**：深度适配 Fabric Loader 初始化与服务准备日志链路；
    - **Forge & NeoForge 模组服**：适配模组加载阶段与专用就绪日志标记（如 `Minecraft has finished loading`、`Server level "world" is being initialized` 等）。
  - **进程互锁与防并发误操作**：
    - 在实例处于「启动中」或「停止中」的过渡阶段时，界面启动/停止/重启按钮智能锁定并展示加载等待状态，彻底消除重复点击导致的进程冲突或指令竞态异常；
    - 实例处于「停止中」时智能拦截控制台指令输入并给予友好提示，防止无效指令透传。
  - **全链路 UI 动态感知与呼吸灯动效**：
    - 仪表盘 (Dashboard)、实例列表 (Instances)、实例详情控制台 (InstanceManager) 及侧边栏 (Sidebar) 状态灯与徽章全面联动；
    - 新增橙黄色脉冲呼吸灯动效，直观传达过渡期状态；
    - 完整覆盖中英文语言包 (`starting` / `stopping` / `running` / `stopped`)。

- **FRP 内网穿透插件重大升级与体验进化 (v1.1.0)**：
  - **INI 与 TOML 双格式深度支持与自由切换**：
    - 创建与编辑穿透配置时支持自由选择保存为标准 `TOML` 格式或经典 `INI` 格式，完美兼容老版本 FRP 核心与各类第三方穿透平台；
    - 配置卡片网格新增高质感格式胶囊徽章（`TOML` / `INI`），清晰辨识当前配置所用语法规范；
    - 后端存储架构重构升级，在切换格式时自动更新对应扩展名并无缝清理历史异构残留文件。
  - **高兼容性的一键文件导入与剪切板导入（100% 原始数据无损保留）**：
    - 针对 SakuraFrp、ME Frp、OpenFrp、StarryFrp 等各类主流穿透平台服主的使用习惯，支持一键上传导入本地 `.ini`、`.toml`、`.conf`、`.txt` 等配置文件；
    - 支持一键从剪切板读取并导入配置，并在浏览器剪切板权限受限时自动唤起手动粘贴兜底模态框；
    - 内置智能格式嗅探器（自动识别 `[common]`、`[[proxies]]`、`server_addr` 等特征），导入后默认锁定高保真文本编辑模式，杜绝传统表单序列化时对非标字段的丢弃；
    - 升级双向互转引擎，全面收集并回写 `user`、`transport.tls.*`、`use_encryption`、`use_compression` 等穿透平台专有参数及注释，表单与文本自由切换零数据损毁。
  - **运行日志独立模态框弹窗展示与全生命周期持久化**：
    - 彻底重构运行日志展示架构，移除原页面底部固定的内嵌日志面板，改为在各配置卡片与编辑窗口内点击“运行日志”按钮后以居中独立模态框（Modal Dialog）形式弹出；
    - 独立日志对话框支持实时 Socket 流式推送、自动滚屏跟随、一键复制到剪切板、清空日志以及多配置独立隔离；
    - 后端重构日志生命周期，引入独立日志历史字典 `frpLogsHistory`，进程停止或退出后日志依然完整留存，方便随时排查故障。
  - **三层立体加固的多语言国际化体系 (i18n)**：
    - 彻底根除 FRP 客户端下载与版本管理弹窗中显示的未翻译原始键名（如 `frp.use_github_proxy`、`frp.proxy_tips` 等）；
    - 建立「插件独立语言包 (`locales/`) + 宿主全局字典扩充 + 组件内嵌双语字典降级」三层防护机制，即使在极端旧版缓存或脱机场景下也绝不展示未翻译裸键；
    - 全面覆盖中英文语言包，支持所有新增导入、格式转换、剪切板以及日志管理交互提示。
  - **官方商店同步上架与版本升级**：
    - 插件版本号正式递增为 `1.1.0`，同步更新 `docs/plugins_shop/plugins.json` 索引并重新构建打包发行包（`mc-panel-plugin-frp.zip`），便于用户一键检测更新。

### 🛠️ 优化与修复

- **在线玩家全生命周期多组件数据同步与显示修复**：
  - **全周期主动拉取与实时同步**：修复前端在页面加载、实例切换或刷新后未拉取在线玩家名单，导致实例详情主界面显示有在线人数（如 1 人），但在「玩家列表」和「服务器地图」中却显示 0 人的缺陷；
  - **新增 `/list` 控制台响应实时解析**：在后端控制台日志监听中增加对 `/list` 响应（支持 `There are X of a max of Y players online: ...` 及中文服格式）的精准捕获与在线玩家集合自动校准，并实时广播更新；
  - **扩充进出服与掉线特征兼容**：兼容 `[Not Secure]` 消息签名、中文语言包（`加入了游戏` / `离开了游戏`）、超时/断开掉线（`lost connection: Disconnected`）以及基岩版/特殊符号玩家名；
  - **服务器地图插件 (mc-panel-plugin-map) 修复**：修复地图前端组件错误引用 `store.activeInstanceId` 导致向后端请求错误实例 ID 的问题，并在 `store` 中增加代理兼容；同步更新官方商店地图发布包。

- **FRP 运行日志展示与持久化修复**：
  - 修复原点击“运行日志”按钮无响应或在页面底部错误渲染嵌入卡片的问题；
  - 修复原后端在 FRP 进程退出时直接销毁日志导致停止后查询日志为空（“暂无数据”）的缺陷。

- **FRP 插件版本管理对话框深色模式视觉体验修复**：
  - 彻底修复深色主题（Dark Mode）下 FRP 版本下载弹窗中代理设置区域出现的高反差浅灰大底色、白色图标框以及文字对比度不足等显示异常；
  - 采用基于系统 CSS 变量（`--c-surface-elevated`、`--c-border` 等）的现代内嵌微质感卡片设计，输入框与预设徽章完美融入暗色与浅色主题；
  - 为版本列表表格增加圆角封边外框（`rounded-3 overflow-hidden`），全面提升界面的精致度与一致性。

- **插件路由出口精确定位与全局插件独立渲染修复**：
  - 修复全局型插件（如 FRP 穿透管理等声明为 `location: 'global'` 的插件）因原模板条件误判 `!store.currentInstanceId`，导致在存在活跃实例时错误落入「实例详情界面」路由出口并被错误挂载实例侧边栏的异常；
  - 严格区分 `isGlobalPluginView` 与 `isInstancePluginView`，保障全局插件在顶级独立容器（`container-xxl`）中完整舒展渲染，实例插件安全驻留实例控制台内；
  - FRP 管理页返回按钮智能记忆前序视图来源（`store.prevView`），平滑退回原操作页面。

- **卷轴工坊 (Scroll Workshop) 市场与安装流程重构**：
  - **双阶段安装与授权协议确认**：对齐插件市场的安全安装规范，卷轴安装新增协议同意与预览确认流程，防止误操作或安装风险脚本；
  - **交互体验修复**：修复卷轴安装成功后安装弹窗未自动关闭的偶发逻辑缺陷；
  - **线上导航优化**：修复插件列表与卷轴列表右上角“开发文档”错误跳转至本地 `localhost` 的问题，统一重定向至 GitHub Pages 官方在线文档；新增“官方商店”直达入口；
  - **国际化 (i18n) 查漏补缺**：全面清理前端界面中遗留的未翻译文本与无用预设，确保多语言体验纯净完整。

---
## [2.4.1] - 2026-09-11

### ✨ 新功能

- **全新免侵入自动化脚本体系——「卷轴 (Scroll)」与「📜 卷轴工坊」**：
  - **全核心通用与免侵入设计**：无需在 Minecraft 服务端中安装任何 Mod 或 Plugin，纯净服（Vanilla）、模组服（Fabric / Forge / NeoForge）、插件服（Paper / Purpur）乃至基岩版全版本兼容。直接通过面板控制台事件感知与安全指令透传，彻底根除内核版本冲突与服务器崩溃风险。
  - **沙盒运行与独立生命周期**：运行在面板独立的 Node.js 事件循环沙盒中，具备完善的热加载（Hot-load）、热停用、热重载（Hot-reload）及深度销毁机制，在禁用或重载时自动清理所有关联定时器，防止幽灵任务驻留。
  - **全面丰富的控制台事件分发**：
    - `onPlayerJoin(player)`：监听玩家加入服务器事件；
    - `onPlayerQuit(player)`：监听玩家离开服务器事件；
    - `onChat(player, message)`：捕获玩家聊天内容（可快速实现聊天自定义指令如 `!spawn`、`!help` 等）；
    - `onDeath(player, deathReason)`：捕获玩家阵亡事件与死因；
    - `onLog(rawLog)`：捕获控制台每一行原生日志，支持自定义关键词触发；
    - `schedule(intervalMs, callback)`：安全的生命周期绑定定时调度任务。
  - **开发者友好型 DSL 上下文 API**：
    - `scroll.sendCommand(cmd)`：安全控制台指令注入；
    - `scroll.sendTitle(player, { title, subtitle, color })`：便捷全屏大标题展示；
    - `scroll.sendActionBar(player, text)`：快捷栏轻量通知；
    - `scroll.broadcast(text)`：全服聊天框彩色广播；
    - `scroll.storage`：内置键值对持久化存取；
    - `scroll.config`：动态获取服主在前端界面配置的自定义参数；
    - `scroll.logger` 与 `scroll.sleep(ms)`：标准化日志与异步等待工具。
  - **三款官方开箱即用预置卷轴 (`scrolls/`)**：
    - 🌟 **新手迎宾大标题 (`welcome-scroll`)**：玩家进服时在屏幕中央显示金色 Title 大标题与 Subtitle 副标题，并在聊天框向全服发送欢迎广播，支持在前端面板自定义欢迎词与颜色。
    - 🧹 **智能扫地姬 (`auto-cleaner`)**：周期性自动清理地面掉落物（默认 30 分钟），清理前 60 秒与 10 秒向全服发送倒计时广播预警，防止误删贵重物品。
    - 🛡 **健康防沉迷守护 (`health-guardian`)**：自动记录每位玩家的连续在线时长，到达预设阈值（如 1 小时、2 小时）后，自动通过屏幕下方 ActionBar 柔性提醒防沉迷与护眼。
  - **现代化前端「卷轴工坊 (Scroll Workshop)」管理面板**：
    - **卷轴库**：卡片式浏览已安装卷轴，一键启停、状态诊断，支持根据 `configSchema` 动态生成可视化表单，服主无需代码基础即可修改各项运行参数；
    - **在线工坊**：支持在浏览器中直接查看、编辑卷轴的 `index.js` 代码与 `scroll.json` 清单，提供一键载入标准事件模板与热保存即时生效功能；
    - **开发指南**：内置详尽的中文《卷轴开发手册》、生命周期规范、事件监听器与 API 示例代码，引导用户轻松编写专属脚本。
  - **导航与多语言集成**：在主侧边栏增加「📜 卷轴工坊」入口，全面适配中英文国际化（i18n）。

### 🛠️ 优化与修复

- **玩家头像系统全面重构与多源智能容灾**：
  - **解决外网头像源 500 与加载失败异常**：彻底重构前端 `Avatar.js` 组件，构建多源智能降级流水线（`Crafatar` -> `Minotar` -> `MC-Heads` -> `Mojang Session Server`），单个源不可达或响应异常时毫秒级自动切换至备用源。
  - **2D 纯正面头像展示优化**：默认统一采用 2D 纯正面高精度渲染，避免 3D 等距视角下的偏移失真。
  - **100% 本地矢量像素兜底**：若所有外部源均不可达或处于离线环境，系统自动根据玩家用户名/UUID 确定性生成经典的 Steve / Alex 纯矢量像素 SVG 头像，实现零网络依赖的 100% 渲染成功率。
  - **前端内存级缓存**：新增 UUID 映射缓存与最优有效源缓存，大幅减少向第三方头像 API 的重复冗余网络请求。

- **服务器地图稳定性加固与高斯模糊优化**：
  - **修复 Vue 异步更新与 DOM Patch 冲突**：根治地图在玩家列表高频轮询时偶发的 `TypeError: Cannot set properties of null (setting '__vnode')` 与 `Cannot read properties of null (reading 'parent')` 异常。
  - **视觉体验提升**：适度调降了地图界面的高斯模糊强度，保留背景氛围感的同时大幅提升地图纹理与细节的清晰通透度。

- **扩展支持与开发体验**：
  - 增加官方 `types/plugin-api.d.ts` 类型定义，为插件与扩展模块开发提供更加完备的 TypeScript 类型补全与智能感知支持。

---
## [2.4.0] - 2026-07-31

### 🚀 性能优化与稳定性提升

- **服务器地图插件 (mc-panel-plugin-map) 彻底重构与性能深度调优**：
  - **Node.js 事件循环非阻塞异步化**：重构了 Region 区域 MCA 与区块 NBT 读取管线，全量使用 Promise 异步非阻塞 I/O 及异步解压（`zlib.gunzip` / `zlib.inflate`），并在渲染高密度 1024 个区块时引入微任务分片调度（`setImmediate`），彻底根除因主线程同步密集运算导致的面板卡死与请求超时问题。
  - **全局并发渲染队列与防重去重机制**：新增了针对 `/render` 的并发控制槽与正在进行的 Promise 复用机制，避免前端多个区域并发请求时瞬间吃满单核 CPU。
  - **多级智能内存缓存**：
    - **玩家数据缓存**：引入针对 `playerdata/*.dat` 文件的 `mtime` 内存缓存与 `usercache.json` 缓存，仅在文件修改时重新解压解析，将原本高频全量磁盘 I/O 降低 99%。
    - **生物群系缓存**：对 Biome 查询引入区块级 LRU 内存缓存，极大减轻鼠标悬停滑动时的计算开销。
  - **控制台日志过滤优化**：在 `onLog` 日志监听中加入毫秒级快速前缀过滤，避免对全量控制台输出进行无谓的复杂正则回溯。
  - **前端 Leaflet 图层管理与轮询优化**：
    - 移除原本 15 秒全图强制重新请求的激进轮询定时器，改用按需加载与精准 `mtime` 探测。
    - 优化图层切换时的 Layer 复用与内存释放，防止老图层驻留导致的浏览器内存泄漏。
    - 增加 Biome 查询的区块级去重防抖，显著提升地图浏览流畅度。

### ✨ 新功能

- **多级精细化子账号管理与权限控制系统 (Sub-accounts & Detailed Permission Management)**：
  - **子账号数据持久化与安全散列存储**：新增 `/data/users.json` 以存储子账号列表。使用基于 PBKDF2 的随机盐哈希与密文分离存储方案，保障所有子账号凭据达到与主账号相同的企业级防拖库安全标准。
  - **基于会话的权限检查中间件 (requirePermission)**：编写了针对后端 API 的 `requirePermission(permission)` 拦截中间件。非管理员会话会自动对其所拥有的 `permissions` 列表进行严格检查，阻止越权访问。
  - **独立 2FA 双重身份验证支持**：为子账号引入了独立的 2FA 校验逻辑，子账号可以单独配置并绑定各自的 Google Authenticator 等动态口令密钥，登录支持密码 + 2FA 二次分步核验流程。
  - **全线 API 接口精细拦截**：将原有所有暴露的 Builtin 和插件 API 统一进行了权限收紧拦截：
    - 实例操作：`instance.control`（启动、关闭、指令透传）
    - 实例配置：`instance.properties`（修改属性配置、上传图标）
    - 文件管理：`instance.files`（浏览、重命名、读写、打包、解包文件）
    - Mod 管理：`instance.mods`（搜索、下载、启用或禁用 Mods）
    - 玩家管理：`instance.players`（白名单、Ban、OP 列表以及玩家背包查看）
    - 备份管理：`instance.backups`（创建、回档及删除备份）
    - 面板设置：`panel.settings`（修改系统配置、重启面板、更新主管理员 2FA 及密码）
    - 插件管理：`panel.plugins`（管理扩展插件的启用、禁用、安装及卸载）
  - **第三方插件动态权限映射拦截器**：重构了 `pluginLoader.requireAuth` 拦截机制，根据请求的 `pluginId` 动态判定匹配所需的精细化权限（例如：备份插件关联到 `instance.backups`，Modrinth 关联到 `instance.mods`，其他管理插件关联到 `panel.plugins`），实现零修改侵入式的第三方插件安全保护。
  - **侧边栏导航动态权限过滤与看门狗重定向 (Sidebar Permissions Filter & Router Watchdog)**：
    - 前端侧边栏按当前子账号所拥有的权限，动态渲染和隐藏有权/无权访问的 Builtin 和插件侧边栏菜单。
    - 结合 App 路由层对 `store.view` 进行权限 watch 监视，任何试图通过越权跳转或直接篡改状态的操作都会被立即阻断并重定向至 `dashboard` 主页。
  - **子账号 CRUD 交互界面与卡片悬浮翻转动效 (Sub-account CRUD UI & Card Hover-Swap)**：
    - 在面板设置中动态追加“子账号管理”选项卡（仅主管理员可见）。
    - 重新定制了优雅的 Hover-Swap 反转动效卡片用于列举子账号信息：正面直观显示子账号名、2FA 绑定徽章；鼠标悬停翻转至背面，淡入展示拥有的精细化权限列表及编辑、删除操作键，极具现代交互感。
    - 引入了创建子账号、编辑密码与权限、配置并绑定 2FA 的完整弹窗表单交互。

---
## [2.3.1] - 2026-07-30

### ✨ 新功能与修复

- **全新面板设置界面侧边栏选项卡化重构 (Sidebar-Tabs Settings Layout)**：
  - 弃用了原本垂直堆叠且略显杂乱的多卡片网格布局，将面板设置界面完全重构为现代、扁平、高质感的**侧边栏 + 内容选项卡 (Sidebar + Tab Panels)** 结构。
  - 划分为基本设置 (Basic)、安全与认证 (Security)、账户管理 (Account)、集成与回调 (Integrations - Webhook & AI Settings) 以及个性化外观 (Appearance) 五大板块，大幅缩短信息视距，避免滚动疲劳。
  
- **插件管理卡片 Hover-Swap 交互适配 (Plugin Cards Hover-Swap)**：
  - 将实例卡片上广受好评的鼠标悬停交互（Hover-Swap）无缝扩展应用到了插件管理页面中的插件卡片上。
  - 常态下，插件卡片底部只显示优雅简洁的插件状态标志与类型信息（例如：本地插件 / 网页绑定），净化卡片底部排版；鼠标悬停卡片时，该信息会自动淡出并滑升，瞬间显现出整套操作按钮组（检测更新、独立配置、启用/禁用开关、导出、删除），既保证了界面的极简纯净，又保障了高效的操作体验。

- **侧边栏导航高亮指示条重构 (Sidebar Nav Active Bar Redesign)**：
  - 移除了容易被部分浏览器因容器圆角而产生 `overflow: hidden` 裁剪的绝对定位 `::before` 伪元素。
  - 改用原生 `border-left` 边框结合“单侧直角”设计，在激活状态下使按钮左侧保持硬朗纯正的指示线段，而右侧仍然保留圆角，从根本上解决指示条截断或位置错位的问题。

- **面板设置深色模式左侧栏美化 (Panel Settings Dark Sidebar Visual Polish)**：
  - 移除了写死的浅色及硬编码行内背景样式，完全提炼并重构为 `.settings-sidebar` 专有样式。
  - 深色模式下采用精美的半透明暗色叠层 `rgba(0, 0, 0, 0.16)`，完美烘托出暗色调的层次感。
  - 为选项卡按钮重新定制了 `.settings-tab-btn` 样式，在悬停和激活态提供更通透的微光背景与投影效果。

- **按钮悬停裁剪修复与缓存规避 (Overflow Clipping & CSS Cache Busting)**：
  - 限制了滑动容器 `.card-hover-swap-container` 内按钮的 hover 浮动和阴影扩展，规避边界截断。
  - 为全局样式表链接添加了版本后缀（`style.css?v=2.3.2`），强制客户端浏览器立即拉取最新布局。

---
## [2.3.0] - 2026-07-30

### ✨ 新功能

- **全新全局浅色模式配色重构 (Premium Light Theme Refactoring)**：
  - 彻底移除了原有淡绿色基调的浅色配色，重构为业界主流的高端浅蓝灰（Slate / Light-Blue Gray）底色与灰色边框体系。
  - 将浅色模式下的 Primary 品牌色统一调整为与深色模式高度一致的高端科技蓝，并调整了全局卡片投影、微光渐变与毛玻璃（Glassmorphism）容器阴影，大幅提升了白天的视觉美感与现代设计感。

- **实例卡片悬停滑升交互与规格详情徽章 (Hover Swap Cards Interaction)**：
  - 移除了原先右上角的椭圆形快速操作按钮层，净化卡片主区。
  - 默认状态下隐藏操作按钮组，转而通过一组非常轻量美观的微透气泡徽章（Badges）直观显示当前服务器的加载器类别（Vanilla / Fabric / Forge / NeoForge）、匹配的 Java 运行环境（如 Java 17/8）以及绑定监听的本地端口。
  - 鼠标悬停时，徽章面板优雅地淡出并滑升，同时底部的操作按钮组滑上升起，带来极为流畅的微动效反馈。

- **去中心化插件自主更新系统 (Decentralized Plugin Updates)**：
  - **解耦商店机制**：清空并移除了内置的集中式插件市场和 `market.json` 注册列表，赋予用户与开发者完全自由的分发生态。
  - **Manifest 配置扩展**：在 `plugin.json` 清单中支持声明 `updateUrl` 属性，将查询及更新的主动权交由各独立插件开发者掌控。
  - **非 CORS 限制安全代理**：由于浏览器访问第三方域存在跨域安全限制，在后端增加了代理接口 `/api/plugins/check-update` 与 `/api/plugins/update-remote`，由 Node 后端统一代理更新拉取、临时 ZIP 下载、自动静默卸载、热重载以及版本替换。
  - **专属前端更新弹窗**：在前端插件卡片中直观地加入了“检查更新”触发器；结合 Teleport 模态框实现新旧版本号直观比对、changelog 更新日志自动渲染以及一键热升级。

- **全局事件 Webhook 订阅回调机制**：
  - **多通道通知**：在“面板设置”中引入了全新的 **Webhook 订阅卡片**，允许服主按行配置多条外部订阅 URL。
  - **核心事件异步派发**：每当系统发生启动、关闭、服务器崩溃、备份成功、玩家加入或玩家离开等关键事件时，面板后台会自动异步向所有配置的 URL 发起包含详细上下文 JSON 数据的 POST Webhook 回调，便于与外部聊天机器人等工具联动。

- **本地命令行管理工具 CLI Helper (`bin/cli.js`)**：
  - **无缝本地直管**：在项目 `bin/` 目录下引入了本地 CLI 脚本工具，服主可以在宿主机终端直接运行 `node bin/cli.js` 对面板进行管理。
  - **多命令操控**：支持实例列表列举 (`list` / `ls`)、状态查询 (`status`)、启动 (`start`)、优雅停止 (`stop`)、强制结束 (`force-stop`) 及指令透传发送 (`cmd`)。
  - **安全性保证**：在面板启动时随机生成 `cliSecret` 密钥，CLI 与面板通信时通过专有 Request Header 进行密钥对校验，确保本地调用的极致安全性。

- **端口冲突智能检测与引导一键修复**：
  - **启动预警**：当 MC 实例因配置的主机端口被其他进程占用而发生启动冲突时，后台自动捕获该异常状态。
  - **一键重分配引导**：前端会在管理实例页面中弹出冲突引导窗口，支持服主一键随机重新分配可用端口并瞬间写入 `server.properties` 且重新唤醒实例。

- **小白引导式一键实例创建**：
  - 在创建实例向导中添加了一键推荐预设（Vanilla 原生、Spigot、Paper 等），自动根据选择一键拉取默认常规参数配置，无需服主逐项翻阅配置项。

- **敏感确认弹窗的“不再提示”持久化**：
  - 在删除文件、停止实例、文件重置等高频且敏感的操作确认弹窗中，全面引入了“不再提示”复选框。一旦勾选，选项将自动在客户端 `localStorage` 中持久化保存，免去重复点击。

- **白名单与 OP 列表的 3D 玩家头像徽章 (3D Skin Badge)**：
  - 重构了全局玩家头像组件 `<avatar>`，能够对输入的非 Mojang ID 离线昵称进行 PlayerDB 异步代理查询，自动拉取其对应正版 Minecraft 账号的 Mojang UUID。
  - 调用 Crafatar 动态生成带有深度感的 **3D 玩家头部模型模型**并以精美微光徽章形式嵌入白名单与 OP 表格，为面板界面增添独特的个性化游戏代入感。

- **后台性能图表 CSS Grid 自适应布局与视觉优化**：
  - 全面使用 CSS Grid 对 Dashboard CPU/Memory/Storage/JVM 性能看板进行了等高自适应重构，即使在各种超宽屏或小屏移动设备上均拥有绝佳的排版显示。
  - 引入了运行状态呼吸灯、卡片悬浮时的平滑微移微调与迷你操控栏浮现动效，并彻底清除了 template 中的 CSS side-effect 编译报错警告。

- **插件开发指南同步升级**：
  - 同步修改了面向开发者提供的面板内置 `docs/dev-guide` 插件开发说明文档。在中英文 `plugin.json 清单参考` 表格中，均补充编写了 `updateUrl` 选项的使用方法、代理工作模式以及期待的 JSON 响应格式规范。

---
## [2.2.6] - 2026-07-26

### ✨ 新功能

- **多通道认证共存与无缝切换**：
  - **账号密码登录**：新增标准的管理员账号密码登录渠道，支持在初始化引导时安全设定，且 2FA 绑定变为可选。
  - **2FA 令牌直登**：保持原版极简的 2FA 令牌直接登录体验。
  - **切换与首选记忆**：登录框底部动态展示切换入口，允许服主根据偏好在两者间进行无缝切换。系统使用 `localStorage` 自动记录并默认展示最近一次所使用的登录模式。

- **高阻抗防暴力破解机制 (Anti-Brute-Force)**：
  - **内存失败限制**：后台建立 IP 级的登录失败监测，当同一 IP 发生密码、验证码或 2FA 令牌累计 5 次失败尝试时，系统将对该 IP 临时锁死 10 分钟。
  - **动态时间回显**：登录界面在锁死期间会对受限的请求进行拦截，并提示直观的剩余等待解锁秒数。

- **零依赖 SVG 图形验证码**：
  - 在账号密码登录页面中引入图形验证码，防御自动化爆破软件。
  - 采用纯 JS 手绘 SVG 动态图像方案，内部添加干扰曲线、多角度字符旋转与尺寸渐变，具有良好的 OCR 抵抗力，同时完美避免了由于安装 `node-canvas` 原生库所导致的环境编译失败风险。

- **精细化视觉对齐与排版抛光**：
  - **输入框规范化对齐**：统一设定登录页面输入框与验证码图片容器的展示高度为 `50px`。
  - **无缝边框圆角过渡**：重构了验证码拼接处的圆角策略（输入框输入区右侧直角，验证码容器左侧直角，两侧最外沿保留 rounded 圆角），并在 CSS 中优化了 SVG 填满容器的显示表现，使其与上方的输入框外观完全对称和谐。
  - **精炼占位符**：将图形验证码 Placeholder 精简为“验证码 / Captcha”，保证在此类大间距等宽输入框中完美居中，不出现截断及溢出。

- **设置中心安全账号管理**：
  - **更新管理员账户**：在“安全设置”页面新增“账号管理”选项卡，允许服主直接更新管理员用户名，或在检验“当前密码”的前提下，重置/更新管理员密码。
  - **2FA 灵活开闭**：支持服主随时在设置页一键禁用 2FA（恢复纯账号密码登录），或开启 2FA 生成新绑定二维码。

- **服务器图标上传智能自适应转换**：
  - **客户端无损转换**：在“服务器设置 (server.properties)”上传图标时，采用 HTML5 Canvas 机制，自动对用户选取的任意尺寸/格式的图片进行中心正方形裁剪，缩放至精准的 `64x64` 分辨率并导出为标准 PNG Blob 进行上传。
  - **零外部依赖编译**：完全通过浏览器内置多媒体硬件渲染接口完成转换，避免了在服务端引入复杂的 C++ 图片底层库或外部 binary，免去平台编译问题。
  - **多语言通知反馈**：更新上传提示文字，说明支持任意图片文件，并在成功上传后提示“图片已自动裁剪并转换为 64x64 PNG 格式”。

## [2.2.5] - 2026-07-25

### ✨ 新功能

- **模组多版本冲突智能检测与双栏独立解耦对话框**：
  - 新增模组多版本冲突自动扫描，通过 Modrinth 项目 ID 或名称 Slug 智能识别并按模组去重计数。
  - **动态角标预警按钮**：移除顶部常驻横幅，在“上传模组”按钮旁动态显示“冲突模组”预警按钮，带版本冲突组数量角标。
  - **双栏冲突解决对话框**：左栏显示冲突模组（支持独立滚动及高亮切换），右栏显示所选模组所有冲突版本的详细信息与保留动作。
  - **免二次确认删除**：底部新增“选择版本后不要弹出确认提示框”复选框，选项自动持久化至 `localStorage`。

- **多语言支持 (i18n) 兼容**：
  - 将模组冲突处理、模组及文件管理器上传确认弹窗中的全部硬编码中文字符串，完整提取并合并到了中/英文国际化配置文件中，完美兼容多语言切换。

- **系统更新下载体验深度优化**：
  - **下载进度条信息细化**：在系统更新进度条弹窗中，新增实时显示“已下载大小 / 总文件大小”的数据追踪，避免信息过少。
  - **代理重定向穿透**：重构后端下载逻辑，在 Axios 自动跟踪重定向时，手动拦截 3xx 重定向并对目标 URL 重复应用配置的 GitHub 代理加速，完美解决因跳转到 GitHub Codeload 造成代理失效而下载极慢的痛点。

- **模组上传与不合规拦截对话框（主题/光照模式兼容）**：
  - 对“模组更改”页面的双栏上传确认对话框进行了样式优化。移除了硬编码的暗色背景及白色文字类，改用系统标准的 css 主题属性变量 (`var(--c-surface)`, `var(--c-border)`, `var(--c-text-primary)`)。
  - 完美适配**浅色/白昼模式**下的文字对比度，彻底解决了浅色模式下文字不可见的问题。

- **文件管理器上传二次确认**：
  - 在“文件管理”页面中，无论是点击上传还是拖拽上传，在开始传输前均增加了统一的**上传确认对话框**。
  - 支持在列表中勾选/取消勾选个别待上传文件，同样 100% 适配深浅色模式。

- **全局进度条组件主题兼容与细节优化**：
  - 对毛玻璃进度弹窗进行了规范化样式重构，使用 `var(--c-primary)` 适配全局主题强调色（浅色模式下呈现护眼绿，暗色模式下呈现深空蓝），移除了非标准的紫色渐变。
  - 全面采用系统变量渲染标题、处理进度和传输速度，提升浅色模式下的视觉表现。

---
## [2.2.3] - 2026-07-16

### ✨ 新功能

- **服务器拓扑地图与玩家雷达插件**：
  - 新增 `mc-panel-plugin-map` 插件，在左侧菜单集成“服务器地图”入口。
  - **3D 地势拓扑渲染（Hillshading）**：后端采用高性能轻量级 MCA（Region）二进制区块解压扫描算法，读取各区块中的高度图矩阵，根据高度差计算坡度，动态运用明暗光照进行拓扑渲染，形成极具 MC 原生质感的 2D 地形地图。
  - **高度图与水域智能着色**：根据高度分段渲染深海/浅水/草地/山林/雪山地貌。
  - **免依赖 BMP 编码与文件缓存**：手写了零依赖的 BMP 文件结构组装器，直接输出为浏览器可直接渲染的 BMP 图像流，并根据 MCA 修改时间（`mtime`）支持智能高速缓存，避免重复渲染损耗 CPU。
  - **多维度玩家位置追踪**：解析玩家 `playerdata/*.dat` 档案与 `usercache.json` 用户映射，获取其三维坐标和维度信息（主世界/下界/末地），并在前端以 pulsing 粒子雷达定位标示出位置。
  - **玩家点击追踪联动**：在侧栏玩家雷达列表中，点击任意玩家，地图将自动切至该玩家所在的维度、跳转至所属的区块图幅，并直接以玩家为中心聚焦展示。

- **控制台指令助手图形化配置大版本扩充**：
  - 将控制台图形化参数助手支持的原版命令数量从 12 个大幅扩展至 **36 个**，基本覆盖原版 Minecraft 服务器管理的全部常规操作（包含 `/give` 给予物品、`/effect` 效果控制、`/enchant` 附魔、`/recipe` 配方、`/damage` 伤害、`/tick` 刻速率、`/playsound` / `/stopsound` 声音管理、`/worldborder` 边界控制等）。
  - 各类复杂指令支持按键联动过滤，参数自动展开，极大地降低了小白服主运维门槛。

---
## [2.2.2] - 2026-07-16

### ✨ 新功能

- **玩家管理（状态效果）图形化重构**：
  - 将原本需要用户手动敲入指令附加状态效果的 `askEffect` 功能，完全重构为精美的图形化配置面板。
  - 支持状态效果下拉选择（包含速度、缓慢、急迫、力量、隐形、夜视等 30 种 Minecraft 主流状态效果），自动随面板中英文语言自适应切换展示。
  - 支持一键清除目标玩家身上的所有状态效果，支持快捷设定持续时间（30s、1m、5m、30m、永久）与等级（I 级到 X 级或最大级 255），并可勾选“隐藏粒子效果”参数。
  - 动态国际化语言支持：修复了原本使用非存属性 `store.language` 导致语言无法动态切换的缺陷，改用全局 `store.lang === 'zh'` 进行汉化/英文互译。
  - 给予物品弹窗国际化适配：在英文环境下，给予物品列表和选中卡片名称能自动切换为英文（主英文、次中文），更加国际化。

- **给玩家物品弹窗重构（创造模式背包风格）**：
  - 将原本简陋的单行文本框指令给予方式，重构为了类似 Minecraft 原版创造模式背包风格的图形化分发面板。
  - 分类页签与全局检索：增加了“建筑”、“战斗”、“工具”、“食品”、“材料”、“特殊”六大物品页签，并支持通过中英文或 ID 模糊检索上千种游戏方块与物品。
  - 动态参数属性配置卡片：选中特定物品时，会自动展开对应的扩展配置。
    - Potion/药水：支持联动过滤效果与快速选择药水类型/时长（普通、喷溅、滞留等，且支持中英文适配）。
    - Enchanted Book/附魔书：支持下拉选择附魔类型与等级（中文等级名与罗马数字双向对照）。
    - Player Head/玩家头颅：可自定义输入皮肤拥有者游戏名。
    - Enchantable Gear/可附魔装备：可一键勾选“附加附魔属性”，并在下拉菜单中为其绑定对应的附魔属性与等级。
  - 支持手动输入自定义物品 ID（如模组/Mod 中的物品 ID），具备优秀的兼容性与扩展性。
  - 界面样式升级：完全融入全局主题（支持深浅色模式），摆脱了粗糙的 MC 原生灰色箱子框质感，过渡动画与视觉效果更加柔和高级。

- **控制台指令助手图形化参数配置面板**：
  - 重构了控制台底部的“指令助手”点击交互逻辑。当用户点击需要参数的常用指令（如 `gamemode`、`difficulty`、`time`、`weather`、`gamerule`、`clear`、`teleport`、`experience`、`kick`、`ban`、`summon`、`say` 等）时，不再直接填充指令框架，而是弹出一个专属的指令参数配置对话框。
  - 对话框内置多种交互控件：
    - `player_select`：支持一键下拉选择当前在线玩家或常用快捷选择器（@s、@a、@p、@r）。
    - `select`：常规参数的极简下拉选择（如游戏模式、天气类型、游戏规则）。
    - `select_input`：可下拉选择推荐预设值，同时也支持手动自由输入的复合输入框（如游戏时间数值、召唤实体类型）。
  - 新增“实时指令预览”展示：对话框底端配有实时渲染的完整 Minecraft 命令串，帮助用户直观了解参数如何映射拼装为命令。
  - 双重应用方式：支持“填入控制台”（方便进一步调整或追加指令）和“发送指令”（直接向服务器投递执行）两种选择，极大地提升了新手及小白玩家的操作便捷度。

- **文件管理器多选与混合上传增强**：
  - 将原本“上传文件夹”的 input 选择器增加了 `multiple` 参数，支持同时上传多个不同的文件夹，增强了兼容性。
  - 完整优化了多文件、多文件夹混合拖拽上传（Drag & Drop）逻辑，可同时批量上传并递归建立对应的目录结构。
  - 修复了玩家管理界面显示的玩家网络延迟一直为-1的问题

### 🐛 Bug 修复

- **Minecraft 1.20.5+ / 1.21+ 附魔指令报错修复**：
  - 修复了 Mojang NBT / SNBT 解析器在正式版下废弃 `levels` 包装器所导致的解析崩溃问题，命令格式扁平化为 `stored_enchantments={protection:1}` 且舍弃了有冒号冲突的 `minecraft:` 前缀及双引号，100% 避开了指令分发报错的问题。

- **分片上传路径丢失与合并报错修复**：
  - 修复了分片合并阶段（大文件 >100MB）由于缺少层级结构导致文件夹内的超大文件全部丢失相对路径的 Bug。
  - 修复了后端在分片合并完成创建写入流时，因没有提前建立对应子目录结构导致直接报错 `ENOENT` 异常的问题，变更为递归建立目标合并大文件所需的父目录。

## [2.2.1] - 2026-07-15

### ✨ 新功能

- **内置地图备份解耦与插件化**：
  - 将原本内置于实例详情界面的“地图备份”功能完全剥离并拆分为外部扩展插件 `mc-panel-plugin-backup-instance`
  - 删除了“服务器设置”页面下方的“备份策略”配置卡片，简化底层核心功能逻辑
  - 将面板系统的备份功能拆分为外部扩展插件 `mc-panel-plugin-backup`
  - 更新了前端侧边栏（Sidebar）和国际化配置，隐藏内置地图备份入口，统一由系统插件体系接管

- **官网纯前端“插件市场”上线**：
  - 官网文档目录下推出独立的纯前端“插件市场”页面 `docs/market.html`
  - 提供了直观的“系统工具”、“备份管理”、“网络穿透”、“安全管理”四大插件分类过滤，并支持快速搜索和排序
  - 市场界面增加插件的系统接口权限（Permissions）展示，明确指示各插件请求的特殊权限及安全风险

- **插件 ZIP 动态解析与部署**：
  - 移除了所有在 JS 文件中硬编码的静态列表数据，改由在页面初始化时动态 fetch 读取 `docs/plugins_shop/plugins.json` 索引文件
  - 新增编译索引工具 `docs/plugins_shop/update.js`，可在打包或更新包时自动解压 `docs/plugins_shop/*.zip` 的 `plugin.json` 并抓取真实文件大小，全自动化重构索引
  - 在 `package.json` 中新增 `npm run build:market` 一键生成索引命令，支持增删插件后市场数据自动同步
  - 探查 ZIP 压缩包实际物理大小显示在前端，并在卡片和弹窗内均提供了 ZIP 的直接下载链接与手动解包安装步骤

- **面板增加插件市场跳转入口**：
  - 面板内置“插件管理”界面右上角添加了“插件商店”的直达跳转链接按钮
  - 在 `public/js/i18n.js` 中新增了 `market_button` 翻译，支持随面板中英文自适应语言切换

### 🐛 Bug 修复

- **配置重启自动刷新失败修复**：
  - 修复了打包编译版本（caxa）下保存系统配置时，因旧进程释放端口过慢导致前端盲等超时重载失败白屏的问题
  - 后端 `/api/system/version` 返回进程唯一启动时间戳 `startupTime`
  - 前端 `waitForPanel` 轮询探测时自动比对新老启动时间戳，只有在确定新进程成功绑定端口且获取到不同时间戳后才拉起页面重载，实现了更可靠的配置保存热重启体验

---
## [2.2.0] - 2026-06-02

### ✨ 新功能

- **多加载器支持（Fabric / Forge / NeoForge）**：面板从仅支持 Fabric 扩展为同时支持 Forge 和 NeoForge
  - 设置向导新增"选择加载器类型"步骤，安装时可选择 Fabric / Forge / NeoForge
  - 服务器设置页面版本卡片从"Fabric 核心版本"泛化为"加载器核心版本"，显示加载器类型
  - 版本更换弹窗新增加载器类型选择，支持切换不同加载器
  - 实例创建/编辑界面新增加载器类型选择（编辑时不可更改）
  - Modrinth 浏览器默认 loader 过滤根据当前实例的加载器类型自动联动
  - Dashboard 信息栏新增加载器类型显示

### 🔧 后端变更

- 新增 `loaderType` 字段到实例配置、默认配置和 `server-version.json`
- 新增 `/api/loader/current-version`、`/api/loader/versions/mc`、`/api/loader/versions/loader/:gameVersion`、`/api/loader/change-version` API
- 保留旧 `/api/fabric/*` API 向后兼容
- 设置向导 API `/api/setup/versions/mc` 和 `/api/setup/versions/loader/:gameVersion` 支持 `loaderType` 查询参数
- Forge/NeoForge 安装流程：下载 installer jar → 运行 `--installServer` → 清理临时文件
- 服务器启动时根据 `loaderType` 自动检测正确的启动 jar 文件
- 版本自动检测正则新增 Forge 和 NeoForge 日志匹配模式
- 实例状态推送新增 `loaderType` 字段

### 🌐 国际化

- 新增翻译键：`loader_version`、`loader_type`、`select_loader_type`
- 更新翻译：`change_version_confirm`、`version_changing`、`version_change_success` 从"Fabric 核心"改为"加载器核心"

---
## [2.1.8] - 2026-05-19

### ✨ 新功能

- **面板安全关闭保护**：关闭面板或执行更新时，自动检测是否有正在运行的 MC 实例
  - 更新面板时，如果存在运行中的实例，返回 409 错误并提示用户先关闭实例
  - 命令行 `stop`/`restart` 命令执行时，如果存在运行中的实例，面板拒绝关闭并在终端提示
  - 再次按 Ctrl+C 或使用 `--force`/`-f` 参数可强制关闭（不推荐，可能导致数据丢失）
  - 帮助信息新增 `--force` 选项说明

- **备份策略联动**：关闭"启用面板备份"后自动隐藏侧边栏"地图备份"入口并禁用定时备份
  - 侧边栏根据 `backupStrategy` 动态显示/隐藏"地图备份"项
  - 后端切换备份策略为非 `panel` 时自动关闭 `autoBackupEnabled`
  - 前端保存备份策略后同步更新 store 状态，若当前在"地图备份"页面则自动跳转

### 🐛 Bug 修复

- **"移除OP"图标乱码**：修复了玩家操作菜单中"移除OP"按钮图标显示为方格乱码的问题，`fa-regular fa-crown` 改为 `fa-solid fa-crown`（Font Awesome 免费版无 crown 的 Regular 变体）
- **玩家卡片下拉菜单被遮挡**：修复了在线玩家卡片下拉菜单被下方白名单卡片覆盖的问题，菜单打开时给父卡片和玩家卡片同时提升 z-index
- **控制台日志重复显示**：修复了实例详情页控制台每条日志显示两次的问题。全局 `console`/`console_history` 监听器与实例级 `console:${instanceId}` 监听器同时工作导致日志被 push 两次，现改为当已选择实例时仅使用实例级监听器
- **控制台日志丢失**：修复了离开面板后重新进入时历史日志丢失的问题
  - `getOrCreateInstanceState` 初始化时从实例目录的 `panel.log` 文件恢复历史日志
  - 修复前端 `req_history` 传参错误：将 `{ instanceId: newId }` 对象改为直接传字符串 `newId`
  - 后端 `req_history` 增加参数类型检查，确保正确识别实例 ID
  - 修复前端缺少 `console_history:${instanceId}` 监听器，导致后端返回的历史日志无人接收
  - 修复 `enterInstance` 中重复调用 `socket.emit('req_history')` 导致日志重复加载
  - 前端初始化时自动恢复 `currentInstanceId`（从后端 `activeInstanceId`），确保刷新页面后能正确加载实例历史日志
  - `/api/instances/list` 返回格式新增 `activeInstanceId` 字段，前端兼容新旧格式

---

## [2.1.7] - 2026-05-18

### ✨ 新功能

- **在线玩家界面重构**：将在线玩家从表格布局改为响应式卡片网格布局
  - 大屏一行显示 3 个玩家，中屏 2 个，小屏 1 个，自适应视口宽度
  - 每个玩家卡片显示头像、名称、网络延迟
  - 操作按钮收进三点菜单（⋮），点击展开下拉菜单选择操作，减少界面杂乱
  - 白名单列表同步改为响应式网格布局

- **玩家网络延迟显示**：新增玩家网络延迟（Ping）显示功能
  - 后端新增 `GET /api/server/player-pings` API，解析服务器日志和实时命令获取玩家延迟
  - 延迟数值带颜色指示：绿色（<50ms）、黄色（50-150ms）、橙色（150-300ms）、红色（>300ms）
  - WiFi 图标随延迟等级变色

- **扩展玩家管理操作**：在线玩家操作从 6 个扩展到 16 个
  - 新增操作：给予物品、给予效果、给予经验、发送标题、私聊消息、重置出生点、添加白名单、给予OP、移除OP、封禁IP
  - 所有操作均通过三点菜单的下拉菜单访问
  - 需要输入参数的操作自动弹出输入框提示

- **控制台指令助手**：在控制台发送指令区域新增指令文档和快捷发送功能
  - 新增指令助手按钮（终端图标），点击展开/收起指令面板
  - 指令面板包含 80+ 条 Minecraft 服务器指令，按 5 大分类整理：玩家、服务器、世界、管理、高级
  - 支持按分类筛选和关键词搜索
  - 每条指令显示语法和中文说明
  - 常用指令提供快捷发送按钮，一键填入或发送
  - 指令数据来源于 MC 服务器 `help` 命令输出

- **i18n 翻译完善**：新增 80+ 条中英文翻译
  - 新增玩家管理相关翻译：更多操作、给予物品、给予效果、给予经验、发送标题、私聊消息、重置出生点、添加白名单、给予OP、移除OP、封禁IP 等
  - 新增指令助手相关翻译：指令助手、分类名称、搜索提示、快捷指令、80+ 条指令描述（中英文）

### 🔧 优化

- **玩家卡片样式**：新增 `.player-card` 自定义 CSS，优化下拉菜单在卡片内的显示效果
- **指令面板样式**：新增 `.cmd-panel`、`.cmd-list`、`.cmd-item` 样式，指令面板最大高度 350px 可滚动
- **新增 CSS 工具类**：`.min-w-0`、`.text-purple`、`.text-indigo`、`.text-cyan` 用于布局和分类着色

---

## [2.1.6] - 2026-05-18

### ✨ 新功能

- **server.properties 动态编辑器**：重写了服务器设置界面的图形化编辑器，支持所有版本的 Minecraft 服务器配置
  - 移除了硬编码的属性列表，改为从实际 `server.properties` 文件动态解析所有属性
  - 后端新增 `GET /api/server/properties` API，解析文件并返回结构化属性列表（含类型推断）
  - 后端新增 `POST /api/server/properties` API，仅提交修改的属性，保留注释和格式
  - 新增属性类型推断：已知属性自动识别为 boolean/select/number 类型，未知属性默认为 text
  - 新增 10 个分组：基础设置、游戏规则、世界生成、生成控制、性能与网络、安全与远程、资源包、权限控制、管理服务、其他设置
  - 新增搜索/筛选功能，可按属性名或值过滤
  - 未知属性（不在 schema 中的）自动归入"其他设置"分组，确保新版本属性也能编辑
  - Schema 覆盖 60+ 个属性，包含 1.20.x 至 26.x 版本的所有已知属性
  - i18n 翻译键从下划线格式（`max_players`）改为横线格式（`max-players`），与实际属性名一致
  - 新增 5 个分组翻译键和 35+ 个属性标签翻译键（中英文）

- **玩家管理界面重构**：重新设计了玩家管理界面，解决人多时难以使用的问题
  - 在线玩家从卡片式布局改为紧凑表格布局，每行一个玩家，信息密度更高
  - 操作按钮从下拉菜单改为行内图标按钮组，传送/游戏模式/清背包/击杀/踢出/封禁一目了然
  - 新增在线玩家搜索/筛选功能
  - 白名单/OP/黑名单列表也改为表格布局，并新增搜索筛选
  - 黑名单列表新增封禁原因和到期时间列
  - Tab 标签新增图标和数量徽章
  - 添加玩家输入框支持回车键快捷添加
  - 移除了有内存泄漏风险的全局 dropdown 点击监听器

### 🐛 Bug 修复

- **服务器设置卡片布局**：修复了服务器设置界面分组卡片独占一行的问题，改为大屏 2 列响应式布局
- **卡片边框被覆盖**：移除了备份策略和 Fabric 版本卡片的 `overflow-hidden` 和自定义 border-radius，修复了边框被覆盖的问题
- **玩家管理 Tab 显示异常**：修复了玩家管理界面 Tab 标签的布局显示异常问题
- **多语言翻译**：改进了 `query.port`、`rcon.password`、`rcon.port` 等含点号属性的翻译逻辑，新增 `getLabel` 函数支持手动翻译表
- **更新功能架构检测**：修复了更新功能中硬编码 `linux-x64`/`linux-arm64` 的问题，改为自动检测平台（`win`/`linux`/`macos`）和架构组合
- **更新功能 Asset 查找**：改进了更新功能中 Asset 文件查找逻辑，支持精确匹配 → 模糊匹配 → 关键词匹配三级回退机制
- **caxa 可执行文件检测**：改进了 caxa 打包环境下可执行文件路径检测逻辑，支持从多个可能位置查找（当前工作目录、`process.argv[0]` 目录、`process.argv[1]` 目录），并添加 argv 扫描兜底机制

---

## [2.1.5] - 2026-05-17

### 🐛 Bug 修复

- **状态指示器脉冲动画被截断**：修复了实例列表界面中服务器状态为"运行中"时，状态小绿点的脉冲圆环动画左侧被 `overflow` 截断的问题
  - 移除了实例卡片名称容器的 `overflow-hidden` 类
  - 状态显示区域设置 `overflow: visible`
  - 给 `.status-indicator` 增加 `padding: 3px` 和 `box-sizing: content-box`，为动画预留空间

---

## [2.1.4] - 2026-05-15

### 🐛 Bug 修复

- **实例设置对话框布局**：修复了实例详情页右上角设置按钮弹出的对话框中，"服务器启动文件"下拉框和"刷新"按钮换行显示的问题，改为同一行并排显示
  - 将 `input-group` 布局改为 `d-flex gap-2 align-items-center` 布局
  - `CustomSelect` 组件包裹在 `flex:1; min-width:0` 的 div 中自动填充剩余空间
  - 刷新按钮添加 `flex-shrink-0` 防止被压缩
- **JVM 参数修改不生效**：修复了在实例设置对话框中修改 JVM 参数后对服务器运行没有影响的问题
  - **后端**：`/api/instances/update` 中 `javaArgs`/`jarName`/`javaPath` 的更新条件从 truthy 检查改为 `!== undefined`，确保空数组/空字符串也能正确保存
  - **后端**：`/api/server/start` 和新建实例逻辑中，`javaArgs` 回退条件从 `instConf.javaArgs || appConfig.javaArgs` 改为 `(instConf.javaArgs && instConf.javaArgs.length) ? ... : ...`，确保空数组时能正确回退到全局默认值
  - **前端**：Dashboard 的 `saveStartupSettings` 中，移除了 `if (payload.javaArgs)` 判断，改为始终将字符串按行分割并过滤为空数组，确保后端始终接收到数组而非字符串
- **Java 版本显示 "Not Installed"**：修复了当实例使用非系统包管理器安装的 Java（如 Java 管理器下载的 Java）时，实例详情页控制台卡片中 Java 版本始终显示 "Not Installed" 的问题
  - 根本原因：`system_stats` 事件中的 `javaVersion` 只检测全局默认 Java 路径（`appConfig.javaPath`，默认为 `'java'`），未考虑实例可能使用了自定义 Java 路径
  - 在实例状态中新增 `javaVersion` 缓存字段，服务器启动时自动检测并缓存
  - `system_stats` 事件优先使用当前实例缓存的 Java 版本，未缓存时异步检测并回退到全局版本
  - 后端 `/api/system/update_check` 新增返回 `publishedAt`（发布时间）字段

### ✨ 新功能

- **更新日志显示**：检查更新发现新版本时，现在可以查看更新日志和发布时间
  - 新增"发布时间"显示，格式化为本地化日期时间
  - 新增"查看更新日志"按钮，点击展开/收起 Markdown 格式的更新日志
  - 使用 marked.js 渲染 GitHub Release 的 Markdown 正文
  - 更新日志区域限制最大高度 400px，超出自动滚动
  - 新增 i18n 翻译键：`released_at`、`show_changelog`、`hide_changelog`（中英文）
- **服务器强制关闭**：新增强制关闭服务器功能，当 MC 服务器无法被正常 `stop` 命令关闭时可使用
  - 实例详情页右上角新增骷髅图标按钮（仅在服务器运行时显示），点击后弹出确认对话框
  - 确认后发送 `SIGKILL` 信号立即终止服务器进程
  - 后端新增 `/api/server/force_stop` API
  - 控制台日志记录强制终止操作
  - 新增 i18n 翻译键：`force_stop`、`force_stop_confirm_title`、`force_stop_confirm_msg`、`force_stop_sent`（中英文）

---

## [2.1.3] - 2026-05-15

### 🐛 Bug 修复

- **FRP 管理器下载 404 错误**：修复了在中国网络环境下 FRP 插件下载失败返回 404 的问题
  - 新增 `nativeHttpGet` 函数，使用 Node.js 原生 `https`/`http` 模块作为 axios 的备选下载方式，解决打包后 axios 兼容性问题
  - 修复 `applyGithubProxy` 代理 URL 拼接逻辑：旧实现用 `url.replace()` 替换域名，改为正确的代理 URL 拼接方式（`proxy + url`）
  - 重写下载逻辑，添加多源回退机制：依次尝试 GitHub 源站 → 用户配置的 GitHub 代理 → 内置镜像源（ghfast.top、mirror.ghproxy.com、gh-proxy.com），每个源先尝试 axios，失败后用 native HTTP 重试
  - 改进 `/releases` 端点：GitHub API 不可达时自动尝试代理获取版本列表
  - 添加连接超时控制（15 秒），快速失败并回退到下一个源
  - 简化前端下载逻辑，移除冗余的 URL 探测代码（后端已自动处理回退）
- **中文文件名上传乱码**：修复了 `fixFileName` 函数对已经是正确 UTF-8 的文件名进行双重编码的 bug
  - 根本原因：新版浏览器（Chrome/Firefox）发送 `filename*=UTF-8''...`（RFC 5987），busboy 会正确解码为 UTF-8，但 `fixFileName` 仍然对它做 latin1→utf8 转换，导致双重编码
  - 新增字符码点检测逻辑：如果字符串包含码点 > 0xFF 的字符（如中文字符），说明已经是正确的 UTF-8，不需要修复；如果只包含 0x80-0xFF 范围的 latin1 扩展字符，才尝试 latin1→utf8 修复
- **删除遗留前端文件**：移除了 `public/js/components/FrpManager.js`，该文件使用错误的 API 路径 `/api/frp/*`（正确路径为 `/api/plugins/mc-panel-plugin-frp/*`），实际使用的是插件自带的 `frontend/FrpManager.js`

### 🔧 优化

- **FRP 下载日志增强**：每次下载尝试都有详细日志记录（源、方式、状态码、文件大小），方便排查网络问题
- **FRP 下载进度提示**：显示当前正在使用的下载源名称（如"正在从GitHub代理下载..."），用户可感知回退过程

---

## [2.1.2] - 2026-05-14

### 🐛 Bug 修复

- **剪切/复制按钮消息显示变量名**：修复了点击工具栏剪切按钮时，Toast 提示显示原始翻译键而非实际文字的问题，补充了缺失的 `clipboard_msg` 国际化翻译键
- **拖拽上传仅上传单个文件**：修复了从资源管理器多选文件拖入面板时只上传了一个文件的问题，改进了 `handleDrop` 和 `collectFilesFromEntry` 的逻辑，确保多文件和混合文件/文件夹拖拽均能正确处理
- **大目录读取不完整**：修复了 `webkitGetAsEntry` 的 `readEntries` API 一次最多返回 100 条目时，大目录内文件丢失的问题，改为循环读取直到无更多条目
- **中文文件名上传后乱码**：修复了上传含中文文件名的文件后，在面板上显示为乱码的问题。前端在 FormData 中额外传递正确编码的 `fileNames` JSON 数组，后端优先使用该数组而非 multer 解析的 `originalname`
- **ZIP 压缩包中文文件名乱码**：修复了解压含中文文件名的 ZIP 压缩包时文件名乱码的问题。解压时检查 ZIP 条目的 UTF-8 标志位（flag bit 11），未设置时尝试用 GBK 解码文件名（兼容 Windows 中文 ZIP）
- **下载和图片预览缺少 instanceId**：修复了 `downloadFile` 和图片预览 URL 缺少 `instanceId` 参数，导致多实例环境下请求失败的问题

### ✨ 新功能

- **解压对话框交互**：解压文件时现在会弹出对话框，用户可选择解压目标目录，默认为去掉扩展名的同名文件夹（之前是直接解压到同名目录，无交互）
- **压缩/解压进度显示**：压缩和解压操作现在会显示进度条（不确定进度模式），包含操作标题和文件名信息
- **图片预览**：点击图片文件名（PNG/JPG/GIF/WebP/BMP/SVG/ICO）可进入图片预览视图，支持缩放和自适应显示
- **压缩包预览**：点击压缩包文件名可进入压缩包内容列表预览，显示文件名、大小、压缩大小，支持直接点击"解压"按钮
- **新增后端 API**：
  - `GET /api/files/archive-list` — 列出压缩包内的文件列表（支持 GBK 文件名解码）
  - `GET /api/files/preview-image` — 图片流式预览（支持 MIME 类型识别和缓存头）
- **解压支持指定目标目录**：后端 `/api/files/operate` 的 `extract` 操作现在支持 `destination` 参数，允许指定解压目标路径

### 🔧 优化

- **文件图标增强**：图片文件（PNG/JPG/GIF 等）现在显示专属的图片图标（`fa-file-image`），而非通用文件图标
- **fixFileName 更健壮**：改进了文件名编码修复函数，添加安全检查，只在解码结果有效时才替换，避免破坏已正确编码的文件名
- **ZIP 解压路径安全**：解压时对每个条目的输出路径进行路径遍历检查（`startsWith` 验证），防止恶意 ZIP 文件的路径穿越攻击

---

## [2.1.0] - 2026-05-12

### ✨ 新功能

- **插件权限系统**：插件现在需要在 `plugin.json` 中声明所需的权限，运行时进行权限检查
- **插件设置系统**：支持插件自定义设置界面，用户可在面板中配置插件参数
- **插件存储系统**：插件拥有独立的键值对存储空间，支持持久化数据
- **插件事件系统**：插件可监听和触发自定义事件，实现插件间通信
- **插件状态监控**：支持查询插件的运行状态和生命周期
- **插件依赖管理**：插件可声明对其他插件的依赖关系
- **插件 API 版本控制**：引入 API 版本号，确保向后兼容性
- **新增后端 API**：
  - `GET /api/plugins/:id/permissions` — 查询插件权限
  - `GET /api/plugins/:id/settings` — 获取插件设置
  - `POST /api/plugins/:id/settings` — 保存插件设置
  - `GET /api/plugins/:id/storage` — 读取插件存储
  - `POST /api/plugins/:id/storage` — 写入插件存储
  - `DELETE /api/plugins/:id/storage/:key` — 删除插件存储项
  - `GET /api/plugins/:id/status` — 查询插件状态
  - `POST /api/plugins/:id/emit` — 触发插件事件
